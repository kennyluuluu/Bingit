port 8080;

handler static {
  location /static;
  root static;
}

handler static {
  location /static2;
  root meh;
}

handler echo {
  location /echo;
}

server {

  listen 8080;
  echo_root /echo;
  static_root /static,/static2;
}
