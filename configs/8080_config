port 8080;
root .;

handler echo {
  location /echo;
}

handler static {
  location /static;
  root static;
}

handler static {
  location /static2;
  root static;
}

handler status {
  location /status;
}

handler meme {
  location /meme;
  root meme;
  view /view;
}